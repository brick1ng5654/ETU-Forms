# ETU-Forms

ETU-Forms - это веб-сервис для создания форм и опросов с использованием конструктора EtuForm. Сервис позволяет создавать формы с различными типами полей, собирать и анализировать ответы, включая загрузку файлов, с интеграцией с ETU ID и etu.cloud.

---
# Команда
- Аршин Александр Дмитриевич (@skraipy)
- Баймухамедов Рафаэль Русланович (@brick1ng5654)
- Пасечный Леонид Витальевич (@pasechnyi_leonid)





---
# Технологический стек
<table> <thead> <tr> <th align="left">Компонент</th> 
                <th align="left">Технология</th> 
                <th align="left">Назначение</th> </tr> 
        </thead> 
<tbody> <tr> <td><strong>Бэкенд</strong></td> <td>FastAPI</td> <td>Основной API сервиса</td> </tr> 
        <tr> <td><strong>Фронтенд</strong></td> <td>React + JS</td> <td>Пользовательский интерфейс</td> </tr> 
        <tr> <td><strong>База данных</strong></td> <td>PostgreSQL</td> <td>Основное хранилище данных</td> </tr> 
        <tr> <td><strong>ORM</strong></td> <td>1) SQLAlchemy + Alembic <br/>2) SQLModel</td> <td>Работа с БД и миграции</td> </tr> 
        <tr> <td><strong>Конструктор</strong></td> <td>EtuForm</td> <td>Создание и отображение форм</td> </tr> 
        <tr> <td><strong>Хранилище</strong></td> <td>Локально(сервера ЛЭТИ)</td> <td>Хранение загруженных файлов</td> </tr> 
        <tr> <td><strong>Кеш</strong></td> <td>Подумать</td> <td>Сессии, кеширование</td> </tr> 
        <tr> <td><strong>Контейнеризация</strong></td> <td>Docker + Docker Compose</td> <td>Развертывание и изоляция сервисов</td> </tr> 
</tbody> 
</table>

---
# Cроки
Сдача готового проекта не позднее 15 марта 2026 года

# Руководосво по работе https протокола.

1) Для запуска в корне проекта должна находиться папка ssl с ключами и шифром
2) В терминале переходим а корень проекта и прописываем `docker compose up -d --build`
3) через docker ps проверяем наличие контейнера. 
4) По адресу https://localhost будет главная страница проекта, с адреса http://localhost будет редиректить на https.

## Почему может не запускаться и не работать.

У меня была проблема, что nginx был запущен в фоне в wsl, то есть надо было остановить его в wsl, `sudo systemctl stop nginx`.

Если запустился контейнер, но адреса https://localhost не работают:
1) Надо зайти внутрь контейнера (В Windows winpty docker exec -it (id контейнера из docker ps) sh)
2) Далее отправим запрос на наш адрес
    - `curl -I http://localhost` - Должно быть написано, что переадресовано и находиться на https://localhost . 301 Moved Permanently
    - `curl -k https://localhost` - Должно выдать html код страницы

Если все супер, то выходим из контейнера с помощью exit, проверяем наш хост

Повторяем все команды, должны быть абсолютно идентичные выводы, первая команда должна редиректить, а вторая возвращать html код. В дальнейшем будем работать над конфидициальностью ключей и шифров.

## Ключи теперь работаю как docker secrets
Они добавлены в .gitignore, соответственно их не будет в репозитории.